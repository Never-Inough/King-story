

  <!-- Step 3 Add Header -->
<%- include("partials/header.ejs") %>
<h1 id="titre">The King and his daughters!</h1>
<!-- Step 2 - If an adjective and noun are sent to this ejs file, 
    Change the h1 to show the adjective and noun.
    But when the user first goes on the website and there is no adjective and noun, 
    Show the default h1 below -->
  
    <% if (locals.adjective && locals.adjective2 && locals.adjective3 && locals.adjective4 && locals.adjective5 && locals.adjective6 && locals.adjective7 && locals.noun && locals.noun2 && locals.noun3 && locals.superlative && locals.superlative2 && locals.superlative3 && locals.masculineName && locals.masculineName2 && locals.masculineName3 && locals.feminineName && locals.feminineName2 && locals.feminineName3) {%>
      
      <h2>Once upon a time there was a <span style="color:green;"><%= adjective %></span> and <span style="color:green;"><%= adjective2 %></span> King. He lived in a <span style="color:green;"><%= adjective3 %></span> Castle.<br>
       For unknown reason, people called him <span style="color:green;"><%= masculineName %></span> <span style="color:green;"><%= superlative %></span>.<br> He had two daughters: <span style="color:green;"><%= feminineName %> <%= superlative2 %></span> and <span style="color:green;"><%= feminineName2 %> <%= superlative3 %></span>. <span style="color:green;"><%= feminineName %></span> was invited to a <span style="color:green;"><%= adjective4 %></span> festivities,<br> but <span style="color:green;"><%= feminineName2 %></span> prefered taking care of her <span style="color:green;"><%= adjective5 %> <%= noun %></span><br> and avoid exposing herself to the <span style="color:green;"><%= adjective6 %></span> and <span style="color:green;"><%= adjective7 %></span> people and their <span style="color:green;"><%= noun2 %></span>.</h2>  
  
    <% } else { %> 
    <h1>Do you want to read the story of a King 🤴 and his daughters 👸 ?</h1>
    <% } %>

<form action="/submit" method="POST">
  <input type="submit" value="Check it yourself!">
</form>
<!-- Step 3 Add Footer -->
<%- include("partials/footer.ejs") %>
